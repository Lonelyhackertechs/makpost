{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd webmak && pip install -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd webmak && gunicorn webmak.wsgi:application --bind 0.0.0.0:$PORT"
  }
}